import React from 'react';
import { Container, Typography } from '@mui/material';

function Contact() {
  return (
    <Container maxWidth="sm">
      <Typography variant="h4" sx={{ mb: 1, fontStyle: "italic", color: 'red' }}>Testimonials</Typography>
    </Container>
  );
}

export default Contact;